# README #

---

### ASL LIFO Example ###

#### What
- Example that makes use of ASL LIFO component.

#### Why
- Example will show end-users a reference implementation.

#### How
- The user provides the size of LIFO and size of element to use through command line argument.
- The user inputs are validated before proceeding.
- The memory will be allocated (malloc) and LIFO will be created.
- The test covers push/pop from single element up to maximum elements possible.
- The test covers cases of empty and full LIFO.
- The test terminates successfully after all tests are completed (or terminates on first error).
